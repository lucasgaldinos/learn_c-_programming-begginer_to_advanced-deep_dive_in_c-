#include <iostream>

/*funtion that converts decimal to binary
In c++ there's no simple way to do this as it seems
don't really know why*/

void dec_to_bin(int n) {
    // array to store binary number
    int binaryNum[32];

}